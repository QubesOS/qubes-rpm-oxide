[package]
name = "rpm-writer"
version = "0.1.0"
authors = ["Demi Marie Obenour <demi@invisiblethingslab.com>"]
edition = "2018"

[dependencies]
rpm-parser = { version = "0.1.0", path = "../rpm-parser" }
